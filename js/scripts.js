var sentence = (prompt("Enter a sentence"));

var first = sentence.charAt(0).toUpperCase();

var last = sentence.charAt(sentence.length -1).toUpperCase()

var reversed = last + first

var combo = sentence + reversed

var total = sentence.length

var divided = total / 2

var inputedNumber = parseInt(divided)

var letter = sentence.charAt(inputedNumber)

var final = letter + combo

alert(reverseFinal)
